
```

```python Python
import os, requests
requests.get(
      "https://proxy.agentroot.app/calendly/scheduled_events",
      headers={
        "Authorization": f"Bearer {os.environ['AGENTROOT_TOKEN']}",
        "X-AgentRoot-ID": os.environ["AGENTROOT_AGENT_ID"],
      },
    )
```
</CodeGroup>

## Full endpoint catalog

The complete Calendly endpoint catalog is callable through the AgentRoot proxy using the pattern above — substitute the path from the upstream's API reference. Full per-endpoint accordions with action-class annotations are landing iteratively in this docs site.

**Upstream reference:** [Calendly API documentation](https://developer.calendly.com/api-docs)

## Related

- **[Conventions](../conventions)** — the proxy contract
- **[Action Class Mapping](../action-class-mapping)** — how Calendly routes classify
- **[Overview](../overview)** — the full provider catalog
