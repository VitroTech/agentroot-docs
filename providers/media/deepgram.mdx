
```

```python Python
import os, requests
requests.post(
      "https://proxy.agentroot.app/deepgram/v1/listen",
      headers={
        "Authorization": f"Bearer {os.environ['AGENTROOT_TOKEN']}",
        "X-AgentRoot-ID": os.environ["AGENTROOT_AGENT_ID"],
      },
      json=<binary audio data>,
    )
```
</CodeGroup>

## Full endpoint catalog

The complete Deepgram endpoint catalog is callable through the AgentRoot proxy using the pattern above — substitute the path from the upstream's API reference. Full per-endpoint accordions with action-class annotations are landing iteratively in this docs site.

**Upstream reference:** [Deepgram API documentation](https://developers.deepgram.com/reference/listen-file)

## Related

- **[Conventions](../conventions)** — the proxy contract
- **[Action Class Mapping](../action-class-mapping)** — how Deepgram routes classify
- **[Overview](../overview)** — the full provider catalog
